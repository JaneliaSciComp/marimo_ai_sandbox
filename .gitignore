# Apptainer image artifacts
*.sif
*.img

# pixi environment (rebuilt from pixi.lock)
.pixi/

# runtime writable work directory
work/

# user-local config (copy of conf/config.default.toml; not tracked)
conf/config.toml

# OS / editor cruft
.DS_Store
__pycache__/
*.pyc
